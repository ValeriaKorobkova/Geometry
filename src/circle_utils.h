#ifndef CIRCLE_UTILS_INCLUSION_GUARD
#define CIRCLE_UTILS_INCLUSION_GUARD
#include "circle.h"

float get_area(circle_t circle);
float get_perimeter(circle_t circle);
int is_intersected(circle_t circle_1, circle_t circle_2);

#endif